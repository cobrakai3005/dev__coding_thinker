1. What is React?
   Ans: React is Javascript library for faster interactive UI.

2. Who made React?
   Ans: Facbook
   Jordan Walke founder of React
3. What is Babel?
   babel convert React Code into plain javascript as browser only understand javascript
4. How does Babel convert HTML code in React into valid code?
5. What is ReactDOM used for? Write an example.
   ReactDOM is a library that provides DOM-specific methods for React applications. It serves as the bridge between React components and the actual DOM (Document Object Model) in web browsers. While React itself is platform-agnostic, ReactDOM specifically handles rendering React components to web pages.
6. What packages do you need to import to work with React?
   Babel
7. How do you add React to a web application?
8. What is React.createElement?
   It is Method to create Element just like document.createElement()
9. What are the three properties that createElement accepts?
   It accept type of element that you want make, an object of atrributes you want to provide like id, and the text you want it to show
10. What is the meaning of render and root?
    root is element in HTML , in which you append all element you will create

    render: is Method to render on output
