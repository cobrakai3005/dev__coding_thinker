let arr = new Array(5); //use create empty array of length 5

arr.fill(0); //use fill all the emplty  places with 0
arr[2] = 500;
console.log(arr, arr.length, typeof arr);

//Reverse

let nums = [1, 4, 6, 8, 10];
let numss = nums.reverse();

console.log(nums, numss);
